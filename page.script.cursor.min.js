import { iterate } from "/iterate.module.static.js";

function fixCursor(value) {
    if (value) {
        document.body.style.cursor = "url('res/cursors/hand-pointer.png'), auto";
    }
    if (!value) {
        document.body.style.cursor = "url('res/cursors/plus-outline.png'), auto";
    }
}

let cards = document.querySelectorAll('.card');
let projCard = document.querySelectorAll('.project-card');

document.body.addEventListener('mouseenter', function() {fixCursor(true)});

document.body.addEventListener('mouseleave', function() {fixCursor(false)});

iterate(cards, projCard)